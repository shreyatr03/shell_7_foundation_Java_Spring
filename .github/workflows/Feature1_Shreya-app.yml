# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - Shreya-app

on:
  push:
    branches:
      - Feature1
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://index.docker.io/v1/
        username: ${{ secrets.AzureAppService_ContainerUsername_0598803b3cdd48549c115f468d6e6467 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_1e290fd716674ac18e7b6c88af6a80ef }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: index.docker.io/${{ secrets.AzureAppService_ContainerUsername_0598803b3cdd48549c115f468d6e6467 }}/shreyatr/foundation_course_docker:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'Shreya-app'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_eb2d966a86184e1cb7cfd69a36c6ca89 }}
        images: 'index.docker.io/${{ secrets.AzureAppService_ContainerUsername_0598803b3cdd48549c115f468d6e6467 }}/shreyatr/foundation_course_docker:${{ github.sha }}'